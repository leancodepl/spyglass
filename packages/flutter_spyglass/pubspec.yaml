name: flutter_spyglass
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  spyglass:
    path: ../spyglass

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
