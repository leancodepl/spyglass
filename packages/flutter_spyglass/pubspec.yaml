name: flutter_spyglass
description: "Reliable service locator for Flutter."
version: 0.0.3
repository: https://github.com/leancodepl/spyglass/tree/main/packages/flutter_spyglass
homepage: https://github.com/leancodepl/spyglass

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  rxdart: ^0.28.0
  spyglass: ^0.0.2

dev_dependencies:
  benchmark_harness: ^2.4.0
  flutter_test:
    sdk: flutter
  leancode_lint: ^19.0.1
  nested: ^1.0.0
  provider: ^6.1.5+1
